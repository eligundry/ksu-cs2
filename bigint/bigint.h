class BigInt()
{
public:
	// Initialize bigint to 0
	// Example: bigint number();
	bigint bigint(); 
	// Initialize bigint to size x
	// Example: bigint number(30);
	bigint bigint(int x);
private:
};
