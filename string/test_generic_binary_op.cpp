//  String class test program
//
//  Tests: XXX
//

#include "string.h"
#include <cassert>

//===========================================================================
int main ()
{
    {
        //------------------------------------------------------
        // SETUP FIXTURE
        string  left(X);
        string  right(X);

        // TEST
        RESULT_TYPE result = (left OP right);

        // VERIFY
        assert(result == X);
        assert(left   == X);
        assert(right  == X);
    }

    {
        //------------------------------------------------------
        // SETUP FIXTURE
        string  left(X);
        string  right(X);

        // TEST
        RESULT_TYPE result = (left OP right);

        // VERIFY
        assert(result == X);
        assert(left   == X);
        assert(right  == X);
    }

    {
        //------------------------------------------------------
        // SETUP FIXTURE
        string  left(X);
        string  right(X);

        // TEST
        RESULT_TYPE result = (left OP right);

        // VERIFY
        assert(result == X);
        assert(left   == X);
        assert(right  == X);
    }

    {
        //------------------------------------------------------
        // SETUP FIXTURE
        string  left(X);
        string  right(X);

        // TEST
        RESULT_TYPE result = (left OP right);

        // VERIFY
        assert(result == X);
        assert(left   == X);
        assert(right  == X);
    }

    {
        //------------------------------------------------------
        // SETUP FIXTURE
        string  left(X);
        string  right(X);

        // TEST
        RESULT_TYPE result = (left OP right);

        // VERIFY
        assert(result == X);
        assert(left   == X);
        assert(right  == X);
    }

    {
        //------------------------------------------------------
        // SETUP FIXTURE
        string  left(X);
        string  right(X);

        // TEST
        RESULT_TYPE result = (left OP right);

        // VERIFY
        assert(result == X);
        assert(left   == X);
        assert(right  == X);
    }

    // ADD ADDITIONAL TESTS AS NECESSARY
}

