//  String class test program
//
//  Tests: XXX
//

#include "string.h"
#include <cassert>

//===========================================================================
int main ()
{
    {
        //------------------------------------------------------
        // SETUP FIXTURE
        string  str(X);

        // TEST
        RESULT_TYPE result = str.OP();

        // VERIFY
        assert(str    == X);
        assert(result == X);
    }

    {
        //------------------------------------------------------
        // SETUP FIXTURE
        string  str(X);

        // TEST
        RESULT_TYPE result = str.OP();

        // VERIFY
        assert(str    == X);
        assert(result == X);
    }

    {
        //------------------------------------------------------
        // SETUP FIXTURE
        string  str(X);

        // TEST
        RESULT_TYPE result = str.OP();

        // VERIFY
        assert(str    == X);
        assert(result == X);
    }

    {
        //------------------------------------------------------
        // SETUP FIXTURE
        string  str(X);

        // TEST
        RESULT_TYPE result = str.OP();

        // VERIFY
        assert(str    == X);
        assert(result == X);
    }

    {
        //------------------------------------------------------
        // SETUP FIXTURE
        string  str(X);

        // TEST
        RESULT_TYPE result = str.OP();

        // VERIFY
        assert(str    == X);
        assert(result == X);
    }

    {
        //------------------------------------------------------
        // SETUP FIXTURE
        string  str(X);

        // TEST
        RESULT_TYPE result = str.OP();

        // VERIFY
        assert(str    == X);
        assert(result == X);
    }

    // ADD ADDITIONAL TESTS AS NECESSARY

}

